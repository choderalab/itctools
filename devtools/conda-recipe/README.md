This is a recipe for building the current development version into a conda binary.

The installation on travis and appveyor is performed by building the conda package, installing it and running tests.
