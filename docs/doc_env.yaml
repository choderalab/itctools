name: documentation
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Dependencies
  - numpy
  - openpyxl
  - setuptools
  - matplotlib
  - seaborn

    # Pip-only installs
  - pip:
    - pint

