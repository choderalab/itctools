name: documentation
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Dependencies
  - coverage
  - mock
  - nose
  - numpy
  - openpyxl
  - setuptools
  - matplotlib
  - seaborn

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - behave
    - pint
    - python-coveralls
    - rednose

