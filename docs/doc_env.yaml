name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Dependencies
  - coverage
  - mock
  - nose
  - numpy
  - openpyxl
  - pip
  - setuptools
  - matplotlib
  - seaborn
  - behave
  - pint
  - python-coveralls
  - rednose

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

